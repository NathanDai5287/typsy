> If you are an AI agent, run `scripts/devin-bootstrap.sh` first. It will auto-spawn a worktree if needed and re-invoke you there. Then read `.devin/knowledge.md` and `.devin/PARALLEL_PROTOCOL.md` before doing anything.
